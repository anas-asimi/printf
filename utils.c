#include "main.h"

/**
 * _specifier_extracter - extracte specifier from a string
 * @format: string to extract from
 * @start: start of specifier
 * Return: pointer to extracted specifier
 */
char *_specifier_extracter(const char *format, int start)
{
	return (format);
}
