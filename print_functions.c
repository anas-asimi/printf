#include "main.h"

/**
 * print_character - function that return copies of the string.
 * @x: x.
 * Return: print.
 */

int print_character(char c, int flags)
{
	flags++;
	_putchar(c);
	return (1);
}
